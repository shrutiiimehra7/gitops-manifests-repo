apiVersion: v1
kind: Service
metadata:
  name: angular-frontend
  namespace: default
spec:
  selector:
    app: angular-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort       # NodePort lets minikube expose it to your browser
