apiVersion: v1
kind: Service
metadata:
  name: java-service
  namespace: default
spec:
  selector:
    app: java-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
